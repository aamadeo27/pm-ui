mutation CreateUser($args: UserCreateInput!) {
  create_user(args: $args) {
    id
    name
    email
    active
  }
}

query GetUser {
  current_user {
    id
    name
    email
    role
    active
  }
}
